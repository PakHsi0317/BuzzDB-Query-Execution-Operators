# BuzzDB-Query-Execution-Operators
Implemented core database physical operators in C++ using the iterator model, including selection, projection, sorting, hash joins, aggregation, and set operations. Extended a SQL-like query parser and execution engine to build operator pipelines for efficient relational query processing.
